<Window x:Class="NordControl.Student.ConfirmDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Подтверждение"
        Width="380"
        SizeToContent="Height"
        WindowStartupLocation="Manual"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        FontFamily="Segoe UI Variable, Segoe UI"
        TextOptions.TextFormattingMode="Display"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True">
    <Border Margin="12" Padding="18,16" CornerRadius="16"
            Background="{StaticResource Brush.Surface}"
            BorderBrush="{StaticResource Brush.Border}"
            BorderThickness="1"
            MouseLeftButtonDown="Card_MouseLeftButtonDown">
        <Border.Effect>
            <DropShadowEffect BlurRadius="28" ShadowDepth="6" Direction="270" Opacity="0.22" Color="#0F172A"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Border Width="40" Height="40" CornerRadius="12" Background="{StaticResource Brush.RoseSoft}" Margin="0,0,12,0">
                    <Path Style="{StaticResource Style.IconPath}" Data="{StaticResource Icon.Lock}"
                          Fill="{StaticResource Brush.Rose}" Width="20" Height="20"
                          HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock x:Name="TitleTextBlock" Text="Выход" Style="{StaticResource Text.Subtitle}"/>
                    <TextBlock x:Name="CaptionTextBlock" Text="" Style="{StaticResource Text.Caption}" Margin="0,2,0,0"/>
                </StackPanel>
            </Grid>

            <TextBlock x:Name="BodyTextBlock" Grid.Row="1"
                       FontSize="13" Foreground="{StaticResource Brush.TextSecondary}"
                       TextWrapping="Wrap" LineHeight="20" Margin="0,0,0,16"/>

            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Content="Отмена" Grid.Column="0" Height="36" MinHeight="36" IsCancel="True"
                        Style="{StaticResource Style.GhostButton}" Click="CancelButton_Click"/>
                <Button x:Name="ConfirmButton" Grid.Column="2" Content="Выйти" Height="36" MinHeight="36" IsDefault="True"
                        Style="{StaticResource Style.DangerButton}" Click="ConfirmButton_Click"/>
            </Grid>
        </Grid>
    </Border>
</Window>
