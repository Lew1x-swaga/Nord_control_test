<Window x:Class="NordControl.Student.PinDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Подтверждение выхода"
        Height="190" Width="360"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F8FAFC"
        FontFamily="Segoe UI">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Введите PIN текущего класса для выхода:" FontSize="13" Foreground="#1E293B" TextWrapping="Wrap" Margin="0,0,0,12"/>

        <TextBox x:Name="PinInputTextBox" Grid.Row="1" MaxLength="4" FontSize="18" FontWeight="Bold" FontFamily="Consolas, Segoe UI" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Height="36" Width="140" HorizontalAlignment="Left" Padding="4" BorderBrush="#CBD5E1" BorderThickness="1"/>

        <TextBlock x:Name="ErrorTextBlock" Grid.Row="2" Text="" FontSize="11" Foreground="#EF4444" Margin="0,6,0,0" Visibility="Collapsed"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,12,0,0">
            <Button Content="Отмена" Width="85" Height="30" Margin="0,0,8,0" IsCancel="True" Background="#F1F5F9" BorderBrush="#CBD5E1" Click="CancelButton_Click"/>
            <Button Content="Выйти" Width="85" Height="30" IsDefault="True" FontWeight="SemiBold" Background="#EF4444" Foreground="White" BorderThickness="0" Click="ConfirmButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
