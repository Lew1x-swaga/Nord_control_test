# План оптимизации, устранения дублирования и аудита боевого режима (Nord Control)

> **Цель документа:** Систематизировать доработки по поверхностной оптимизации, устранению дублирования, тестированию в условиях реальной сети (боевой режим), проверке отказоустойчивости и минимизации нагрузки на CPU / RAM / Сеть.

---

## 1. Поверхностная оптимизация кода и устранение дублирования

### 1.1. Унификация и дедупликация логики
- [ ] **Нормализация имен исполняемых файлов (`NormalizeExeName`):**
  - **Проблема:** Логика очистки пути (`Path.GetFileName`) и добавления суффикса `.exe` дублируется в `RamAppBlocker`, `ProcessMonitor`, `InstalledAppsScanner` и `TeacherPreset`.
  - **Решение:** Вынести единый статический метод-хэлпер `ProcessNameHelper.Normalize(string rawName)` в `NordControl.Core.Helpers` с гарантированным регистронезависимым сравнением.
- [ ] **Единый пул буферов для передачи кадров (`ArrayPool<byte>`):**
  - **Проблема:** При захвате 8–12 кадров/сек регулярное выделение новых массивов `byte[]` для JPEG приводит к фрагментации кучи больших объектов (LOH) и частым сборкам мусора (GC Gen2).
  - **Решение:** Использовать `ArrayPool<byte>.Shared` или многоразовые потоки `MemoryStream` при сжатии и отправке кадров в `DxgiScreenCapturer` и `GdiScreenCapturer`.
- [ ] **Оптимизация десериализации JSON:**
  - **Проблема:** Повторное инстанциирование буферов при чтении кадров типа 1 (JSON).
  - **Решение:** Проверить переиспользование кэшированных экземпляров `JsonSerializerOptions` и прямое чтение из `ReadOnlySpan<byte>` во всех обработчиках.

### 1.2. Гармонизация жизненного цикла (`IDisposable` / `IAsyncDisposable`)
- [ ] **Упорядочивание освобождения сокетов и CancellationTokenSource:**
  - Гарантировать корректный порядок закрытия: `cts.Cancel()` $\rightarrow$ `stream.Close()` $\rightarrow$ `client.Close()` $\rightarrow$ `cts.Dispose()` во всех фоновых циклах без выброса необработанных `ObjectDisposedException`.
- [ ] **Предотвращение deadlock при асинхронном TCP I/O:**
  - Проверить использование `ConfigureAwait(false)` во всех библиотечных методах Core и Protocol.
  - Обеспечить таймауты захвата блокировок `SemaphoreSlim` (`WaitAsync(ms)`).

---

## 2. Аудит боевого режима (Field-Testing) и план фиксов (Edge Cases)

### 2.1. Сетевые сценарии и отказоустойчивость LAN
- [ ] **Смена сетевого интерфейса / IP (Wi-Fi $\leftrightarrow$ Ethernet, переподключение роутера):**
  - **Сценарий:** Ноутбук переключился с Wi-Fi 2.4 GHz на 5 GHz или сменил IP по DHCP во время урока.
  - **Ожидаемое поведение:** `ClassClient` в течение 2–3 секунд переходит в `Reconnecting`, заново рассылает UDP-пробы и склеивает сессию по `session_token` без участия пользователя.
  - **Фикс:** Подписка на событие `NetworkChange.NetworkAddressChanged` для превентивного сброса кэша адресов и триггера повторного discovery.
- [ ] **Изоляция клиентов на Wi-Fi роутере (AP Isolation):**
  - **Сценарий:** В школьной сети роутер блокирует широковещательный UDP broadcast.
  - **Решение:** Проверить, что ручной ввод IP учителя в клиенте (`manualTeacherIp`) безупречно отрабатывает прямой TCP-коннект в обход UDP discovery.
- [ ] **Режим сна / закрытие крышки ноутбука ученика (Suspend / Resume):**
  - **Сценарий:** Ученик закрыл крышку ноутбука на 30 секунд.
  - **Ожидаемое поведение:** При выходе из сна сокет рвется $\rightarrow$ статус `Reconnecting` $\rightarrow$ успешный повторный Join без падения приложения.
- [ ] **Ротация и ограничение локальных логов:**
  - Логи пишутся строго локально (`%LocalAppData%\NordControl\logs`), файл не должен превышать 10 MB (автоматическая ротация до 3 архивных файлов, исключающая переполнение диска).

### 2.2. Защита UI Учителя от подтормаживаний (UI Thread Smoothness)
- [ ] **Асинхронное декодирование JPEG на стороне Учителя:**
  - **Проблема:** Создание `BitmapImage` на UI-потоке из `byte[]` с высоким разрешением (1080p/720p) может вызывать микрофризы интерфейса.
  - **Решение:** Декодировать JPEG в фоновом пуле потоков, вызывать `.Freeze()` для `BitmapSource`, и только затем передавать готовый объект в `Image.Source` через Dispatcher.
- [ ] **Сортировка и фильтрация таблицы процессов:**
  - Удобная сортировка по имени процесса / активности, подсветка текущего активного приложения (`active_exe`).

### 2.3. DPI-масштабирование и многомониторные системы
- [ ] **Проверка масштабирования Windows (125%, 150%, 175%, 200%):**
  - Убедиться, что `DxgiScreenCapturer` и `GdiScreenCapturer` захватывают полный физический рабочий стол, а не обрезанный угол из-за DPI-виртуализации Windows.
  - Добавить `app.manifest` с поддержкой `PerMonitorV2 DPI Awareness` в проекты `NordControl.Teacher` и `NordControl.Student`.

---

## 3. Профилирование и оптимизация ресурсов (CPU / RAM / Сеть)

### 3.1. Нормативы нагрузки на ПК Ученика (Student)

| Режим работы | Максимальный CPU (Core i3 / Ryzen 3) | Максимальный RAM | Сетевой трафик |
|---|---|---|---|
| **Покой (Idle / Ожидание класса)** | **< 0.1%** | **< 35 MB** | 0 Кбит/с |
| **Урок без стрима (Teacher смотрит другого)** | **< 0.5%** | **< 45 MB** | < 5 Кбит/с (только heartbeat + процесс-лист раз в 2.5 с) |
| **Активный стрим экрана (8–12 FPS)** | **< 2.5%** | **< 60 MB** | 1.5 – 3.0 Мбит/с |

### 3.2. Ключевые оптимизации фоновых задач:
- [ ] **Оптимизация мониторинга процессов `ProcessMonitor`:**
  - `Process.GetProcesses()` вызывается раз в 2.5 секунды. Исключить повторные аллокации тяжелых модулей (`MainModule`), кэшировать имена системных процессов по PID.
- [ ] **Оптимизация `RamAppBlocker` (интервал 500 мс):**
  - Быстрая проверка: если блоклист пуст $\rightarrow$ мгновенный `Task.Delay` без вызова `Process.GetProcesses()`.
  - Кэширование списка известных безопасных процессов операционной системы.

---

## 4. Очистка репозитория и гигиена кода

- [ ] **Удаление остаточных временных файлов сборки:**
  - Проверить чистоту git-дерева от локальных кэшей (`.suo`, `.user`, бинарных дампов).
- [ ] **Единообразие стиля кода (Code Style & Analyzers):**
  - Добавить базовый `.editorconfig` в корень репозитория (C# 12, табуляция/пробелы, `var` vs явные типы).
  - Включить `<Nullable>enable</Nullable>` и `<TreatWarningsAsErrors>false</TreatWarningsAsErrors>` во всех проектах.
- [ ] **Стресс-тесты и бенчмарки в тестовом наборе:**
  - Добавить тест на устойчивость к 1000 циклов переподключения без утечек дескрипторов сокетов.
  - Добавить бенчмарк кодирования кадра JPEG (< 15 мс на кадр 1280x720).

---

## 5. Пошаговые этапы внедрения (5 Спринтов / Промптов)

1. **Спринт 1: Дедупликация и чистота кода**
   - Вынос `ProcessNameHelper` (единая нормализация имен `.exe`).
   - Очистка повторяющегося кода в `RamAppBlocker`, `ProcessMonitor`, `InstalledAppsScanner`, `TeacherPreset`.
   - Добавление `.editorconfig`.
2. **Спринт 2: Оптимизация памяти (ArrayPool) и асинхронный UI-рендеринг**
   - Пул буферов `ArrayPool<byte>` в захвате экрана (DXGI/GDI).
   - Фоновое декодирование JPEG с `.Freeze()` в `MainWindow` Учителя.
   - Оптимизация `RamAppBlocker` (нулевая нагрузка при пустом блоклисте).
3. **Спринт 3: Высокий DPI и отказоустойчивость сети (DPI Awareness & Reconnect Jitter)**
   - Добавление `app.manifest` (`PerMonitorV2`) в Teacher и Student.
   - Подписка на `NetworkAddressChanged` для адаптации при переключении Wi-Fi / Ethernet.
4. **Спринт 4: Стресс-тесты, бенчмарки и локальное логирование**
   - Добавление тестов на проверку устойчивости при массовых дисконнектах.
   - Добавление проверки производительности захвата и сжатия кадров.
   - Ротация локальных логов в `%LocalAppData%\NordControl\logs` (макс 10 MB).
5. **Спринт 5: Финальный аудит, сборка self-contained и верификация**
   - Прогон полного набора тестов (`dotnet test`).
   - Проверка потребления RAM и CPU.
   - Пересборка автономных бинарников `publish/Teacher` и `publish/Student`.
